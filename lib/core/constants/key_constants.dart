class AppConstants {
  AppConstants._();

  // Keys for SharedPreferences or Secure Storage
  static const String prefsKeyAuthToken = 'prefs_auth_token';
  static const String prefsKeyUserId = 'prefs_user_id';
  static const String prefsKeyThemeMode =
      'prefs_theme_mode'; // e.g., 'light', 'dark', 'system'
}
